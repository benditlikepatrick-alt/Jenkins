pipeline {
    agent any

    stages {
        stage('Pull Code') {
            steps {
                echo 'Pulling code from develop branch'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
